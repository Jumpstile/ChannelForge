{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Jumpstile/ChannelForge/schemas/provider.schema.json",
  "title": "ChannelForge Provider Source Configuration",
  "description": "Shape contract for provider.json / provider.local.json / provider.example.json (see Read-ChannelForgeProvider.ps1). This validates structure only. It does not enforce the URL trust-boundary rules (scheme allowlist, no credentials, no loopback/private/link-local hosts) — those are runtime checks in Test-ChannelForgeSourceUrl.ps1 and must stay there even if this schema is also used in CI.",
  "type": "object",
  "required": ["provider", "sources"],
  "additionalProperties": false,
  "properties": {
    "_comment": {
      "type": "string",
      "description": "Optional documentation note used by template/example files. Ignored by Read-ChannelForgeProvider."
    },
    "provider": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable provider identifier."
    },
    "base_url": {
      "type": "string",
      "minLength": 1,
      "description": "Informational base URL prefix shared by sources. Not read by Read-ChannelForgeProvider; documentation only."
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/source" }
    }
  },
  "definitions": {
    "source": {
      "type": "object",
      "required": ["name", "url", "enabled"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Source name, e.g. 'Sports'. Must be unique within the file."
        },
        "group": {
          "type": "string",
          "description": "Optional grouping label, often the same as name."
        },
        "url": {
          "type": "string",
          "minLength": 1,
          "description": "Provider source URL. Treated as a secret outside example/placeholder files (see docs/reference/SECURITY.md); use https://example.invalid/... in any tracked file."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this source is included in a build."
        },
        "local_playlist": {
          "type": "string",
          "minLength": 1,
          "description": "Optional path (relative to the repository root) to a local .m3u file for this source, used by Build-Lineup.ps1 (see issue #7 Phase 1). There is no HTTP fetch yet, so a source with no local_playlist is skipped during a build, not an error. This schema only checks that a string was given; Build-Lineup.ps1 enforces at runtime (via Assert-ChannelForgeReadPath) that the resolved path stays under data/playlists/ - traversal, an absolute path elsewhere, a UNC path, or a drive root is rejected, not silently read. A real local playlist can contain real stream URLs and must use a *.local.m3u filename (ignored by Git); see data/playlists/README.md."
        }
      }
    }
  }
}
